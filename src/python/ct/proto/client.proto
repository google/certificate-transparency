syntax = "proto2";

package ct;

message SthResponse {
  required string log_server = 1;
  optional uint64 tree_size = 2;
  optional uint64 timestamp = 3;
  optional bytes sha256_root_hash = 4;
  optional bytes tree_head_signature = 5;
}
